import Testing from './Testing'
import { Meta } from '@storybook/react'

const Config: Meta<typeof Testing> = {
  component: Testing,
  title: 'TEsting component',
}

export default Config

export const Default = {
}
